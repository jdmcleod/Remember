doctype html
html data-theme-mode="light"
  = render 'layouts/head'

  body
    = render 'shared/flash'
    = turbo_frame_tag 'modal'
    = turbo_frame_tag 'panel'
    = render 'confirm'
    - if current_user.present?
      = yield

